/**
 * ICS3UC - Mr. J 
 *  
 * Unit 1 Summative Task
 * 
 * Read the README.md file (right-click it and select Open Preview)
 * Only start this after completing the quiz. 
 * 
 * Author: 
 */

'use strict';
const prompt = require("prompt-sync")();

// hidden_number is a random number from 1 to 50
let hidden_number = Math.floor(Math.random() * 50 + 1);
